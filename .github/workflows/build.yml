name: Build
on:
  - push
  - workflow_dispatch
jobs:
  get-info:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout self
        uses: actions/checkout@v4
      - id: get-info
        name: Get info
        run: python get-info.py
    outputs:
      version: ${{ steps.get-info.outputs.version }}
  build-arch-pkg:
    needs: get-info
    runs-on: ubuntu-latest
    steps:
      - name: Maximize build space
        uses: easimon/maximize-build-space@v10
        with:
          root-reserve-mb: 8192
          swap-size-mb: 8192
          remove-dotnet: true
          remove-android: true
          remove-haskell: true
          remove-codeql: true
          remove-docker-images: true
      - name: Checkout self
        uses: actions/checkout@v4
      - name: Create `tmp` directory
        run: mkdir --mode=777 tmp
      - name: Build
        run: >
          docker run
          --pull always
          --env VERSION=${{ needs.get-info.outputs.version }}
          --mount type=bind,src=$(pwd),dst=/mnt/repo
          --mount type=tmpfs,dst=/tmp,tmpfs-size=16G
          --workdir /mnt/repo
          archlinux:base-devel
          ./build.sh || df
      - name: Upload package
        uses: actions/upload-artifact@v4
        with:
          name: arch-pkg
          path: pkg/*.pkg.*
  notify:
    needs:
      - get-info
      - build-arch-pkg
    runs-on: ubuntu-latest
    steps:
      - name: Checkout self
        uses: actions/checkout@v4
      - name: Install dependency
        run: sudo apt-get --assume-yes install python3-python-telegram-bot
      - name: Notify
        run: python notify.py
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_TO: ${{ secrets.TELEGRAM_TO }}
          VERSION: ${{ needs.get-info.outputs.version }}
          SUMMARY_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
